# Maintenance

Sync `src/Version.php`, changelog, tag. gRPC stub changes need wrapper updates (`SpooledGrpcClient`).

| Change | Update |
| --- | --- |
| Workflow job routes | `contracts/parity.md` |
